# General
pnpm-debug.log
node_modules
.DS_STORE
.vscode

# Foundry files
cache
out-via-ir
lcov.info

# Config files
.env

# Avoid ignoring gitkeep
!/**/.gitkeep

# Keep the latest deployment only
broadcast/*/*/*

# Out dir
out
crytic-export

# Fixture generator deps (run `npm install` in test/fixtures to regenerate vectors)
test/fixtures/node_modules
test/fixtures/package-lock.json
